<Window x:Class="Vexa.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Vexa.Controls"
        xmlns:converters="clr-namespace:Vexa.Converters"
        mc:Ignorable="d"
        Title="Vexa Station"
        Height="900"
        Width="1280"
        MinHeight="720"
        MinWidth="1100">
    <Window.Resources>
        <converters:BoolToBrushConverter x:Key="ActiveSegmentConverter"
                                         TrueBrush="#F5F5F5"
                                         FalseBrush="Transparent" />
    </Window.Resources>

    <Grid Background="#E8E8E8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Header -->
            <RowDefinition Height="120" /> <!-- Waveform -->
            <RowDefinition Height="Auto" /> <!-- Toolbar -->
            <RowDefinition Height="*" />    <!-- Editor -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="12,6">
            <StackPanel Orientation="Horizontal">
                <Border Background="#4A90E2" Width="32" Height="32" CornerRadius="4" Margin="0,0,12,0">
                    <TextBlock Text="🎧" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" />
                </Border>
                <TextBlock Text="{Binding AudioPath}" VerticalAlignment="Center" Foreground="White" FontSize="16" FontStyle="Italic" Opacity="0.8" />
            </StackPanel>
        </Border>

        <!-- Waveform -->
        <Border Grid.Row="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="0,1">
            <controls:WaveformControl WaveformData="{Binding WaveformData}" 
                                      CurrentPosition="{Binding AudioPosition}" 
                                      ZoomLevel="{Binding ZoomLevel}" />
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="2" Background="White" Padding="8,4" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Button Content="▶" Command="{Binding PlayPauseCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Play/Pause" />
                    <Button Content="⏸" Command="{Binding PlayPauseCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Pause" />
                    <Button Content="⏮" Command="{Binding RewindCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Rewind" />
                    <Button Content="⏪" Command="{Binding RewindCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Back" />
                    <Button Content="⏩" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Forward" />
                    <Button Content="⏭" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Next Segment" />
                    
                    <Separator Width="1" Margin="8,4" Background="#EEEEEE" />
                    
                    <TextBlock Text="🔈" VerticalAlignment="Center" Margin="4,0" />
                    <Slider Value="{Binding Volume}" Width="100" Margin="4,0" />
                    
                    <Separator Width="1" Margin="8,4" Background="#EEEEEE" />
                    
                    <Button Content="🔄" Command="{Binding LoopSelectionCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Loop selection" />
                    <Button Content="↺" Command="{Binding LoopLastFiveSecondsCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Loop last 5s" />
                    <Button Content="⚡" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Auto-split" />
                    
                    <Separator Width="1" Margin="8,4" Background="#EEEEEE" />
                    
                    <Button Content="-" Command="{Binding ZoomOutCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Zoom out" />
                    <Button Content="+" Command="{Binding ZoomInCommand}" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Zoom in" />
                    <Button Content="🕐" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Time View" />
                    <Button Content="🔍" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="Search" />
                    <Button Content="AB" Style="{StaticResource ControlButtonStyle}" Width="32" ToolTip="AB Loop" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="🔈" VerticalAlignment="Center" Margin="8,0" />
                    <TextBlock Text="Speed:" VerticalAlignment="Center" Foreground="#555555" Margin="4,0" />
                    <ComboBox SelectedValue="{Binding PlaybackSpeed}" Width="70" Margin="4,0">
                        <ComboBoxItem Content="0.5x" Tag="0.5" />
                        <ComboBoxItem Content="1.0x" Tag="1.0" />
                        <ComboBoxItem Content="1.2x" Tag="1.2" />
                        <ComboBoxItem Content="1.5x" Tag="1.5" />
                    </ComboBox>

                    <TextBlock Text="Time:" VerticalAlignment="Center" Foreground="#555555" Margin="8,0" />
                    <TextBlock Text="{Binding FormattedCurrentTime}" FontWeight="SemiBold" VerticalAlignment="Center" Width="45" />
                    
                    <TextBlock Text="Total:" VerticalAlignment="Center" Foreground="#555555" Margin="8,0" />
                    <TextBlock Text="{Binding FormattedTotalTime}" FontWeight="SemiBold" VerticalAlignment="Center" Width="45" />

                    <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource ActionButtonStyle}" Margin="12,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Editor (Paper Style) -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Padding="40">
            <Border Background="White" MaxWidth="800" MinHeight="900" CornerRadius="2" BorderBrush="#CCCCCC" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" />
                </Border.Effect>
                
                <ItemsControl ItemsSource="{Binding Segments}" Margin="60">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsActive, Converter={StaticResource ActiveSegmentConverter}}" Padding="4" Margin="0,0,0,8">
                                <RichTextBox converters:RichTextBoxHelper.BoundText="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0" Background="Transparent" FontSize="14" Cursor="IBeam">
                                </RichTextBox>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </ScrollViewer>

        <Menu Grid.Row="0" Opacity="0" IsHitTestVisible="False">
            <MenuItem Header="_File">
                <MenuItem Header="Open Audio..." Command="{Binding OpenAudioCommand}" />
                <MenuItem Header="Open Session..." Command="{Binding OpenSessionCommand}" />
                <MenuItem Header="Save" Command="{Binding SaveCommand}" />
            </MenuItem>
        </Menu>
    </Grid>
</Window>

